[user]
	name = vandemjh
	email = jack@jackv.dev
[merge]
    tool = meld
[alias]
	d="diff"
	s="status"
	rollback="!f() { git reset --hard HEAD~$1; }; f"
	ca="commit -a"
	c="commit"
	co="checkout"
	p="push"
	b="branch"
	root="rev-parse --show-toplevel"
	diff-files="diff --name-only"
	ignore = "!gi() { cd ${GIT_PREFIX:-.} && curl -sL https://www.gitignore.io/api/$@ >> .gitignore; echo 'gitignore created/updated!'; }; gi"
	ignore-file="!f() { cd ${GIT_PREFIX:-.} && for arg in $@; do echo $arg; done >> .gitignore; }; f"
	last = "log -1 HEAD"
	mute = "!f() { cd ${GIT_PREFIX:-.} && git update-index --assume-unchanged $1; }; f"
	unmute = "!f() { cd ${GIT_PREFIX:-.} && git update-index --no-assume-unchanged $1; }; f"
	quick-commit-push = "!f() { git commit -am \"$(date +'%A %D at %I:%M%p')\" | tee; git push | tee; }; f"
	do-not-track = "!f() { echo $1 >> .git/info/exclude; }; f"
	do-track = "!f() { sed -ie s/$1//g .git/info/exclude; }; f"
	commit-it = "!f() { cd ${GIT_PREFIX:-.} && git commit $(find . -type f -iname $1); }; f"
	cp-prettier = "!f() { cp -n ~/.prettierrc $(git rev-parse --show-toplevel); echo '--- Automatically adding and committing prettierrc file ---'; git add $(git rev-parse --show-toplevel)/.prettierrc; git commit $(git rev-parse --show-toplevel)/.prettierrc -m 'Added prettier configuration'; }; f"
	
